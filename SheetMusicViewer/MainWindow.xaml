<Window x:Class="SheetMusicViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SheetMusicViewer - 楽譜ビューワー" 
        Height="768" Width="1024"
        KeyDown="Window_KeyDown" 
        Background="#222222">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 上部操作バー -->
        <Border Grid.Row="0" Background="#2D2D30" Padding="10,8">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button Content="楽譜(PDF)を開く" Click="OpenFile_Click" Padding="12,6" Margin="0,0,15,0" />
                    <TextBlock x:Name="PageInfoText" Foreground="#CCCCCC" VerticalAlignment="Center" FontSize="14" Text="PDFファイルを開いてください（[→] 次へ / [←] 前へ）" />
                </StackPanel>

                <TextBlock DockPanel.Dock="Right" Foreground="#888888" VerticalAlignment="Center" FontSize="12" Text="F11: 全画面切替" Margin="0,0,10,0" />
            </DockPanel>
        </Border>

        <!-- 楽譜（見開き2ページ）表示エリア -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左ページ -->
            <Border Grid.Column="0" Margin="4" Background="#181818" CornerRadius="4">
                <Image x:Name="LeftPageImage" Stretch="Uniform" />
            </Border>

            <!-- 右ページ -->
            <Border Grid.Column="1" Margin="4" Background="#181818" CornerRadius="4">
                <Image x:Name="RightPageImage" Stretch="Uniform" />
            </Border>
        </Grid>
    </Grid>
</Window>